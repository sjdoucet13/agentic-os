# _graphs — GENERATED, READ-ONLY

Graphify output. **Never hand-edit anything in here** — re-running `graphify update`
**overwrites** these directories. Authored knowledge never lives in `_graphs/`.

| Subdir | Source repo | Serves |
|---|---|---|
| `mad-custom-tx/` | mad-custom-tx | `mad/customs`, `mad/armory` |
| `zus-legacy/`    | zus-legacy (nextjs-boilerplate) | `zus` |

Domains *reference* these graphs by path; the dashboard *links* to them. Freshness is
tracked in `_system/state/graph-freshness.json`. Graphify itself is **not yet
installed** (see `_system/roadmap/ROADMAP.md`).
