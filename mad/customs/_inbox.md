# mad/customs — Inbox (raw captures)

> Append-only CAPTURE surface. RECONCILE promotes items into `knowledge/`.
> CC-written via the `capture` skill; one domain only — never hand-bridged.

- [2026-06-17 15:51] Configurator calibration: verify Konva (client canvas) and Sharp (server composite) agree on the engraving-box coordinate ORIGIN (top-left vs center) when recalibrating LTM7216/GFT220 — a mismatch makes the on-screen proof diverge from the produced engraving.
