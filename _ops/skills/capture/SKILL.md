---
name: capture
description: CAPTURE moment — drop a thought, finding, decision, bug, or task into the correct domain fast, without crossing domain walls.
status: stub
---

# capture (STUB)

Real logic deferred. This is the structural placeholder for the CAPTURE moment
(`_system/OPERATING_PROCEDURE.md`).

## Intended behavior
1. Take a raw capture (text) + a target domain.
2. If the domain is ambiguous, **ask** — do not guess, never bridge two domains.
3. Append it to the domain's capture surface (raw, fast). Reconciliation into
   `knowledge/` happens later in the RECONCILE moment.
4. Compliance-relevant armory items capture **only** under `mad/armory/`.

## Inputs (planned)
- `domain`: one of fugro | mad/customs | mad/armory | mad/_shared | zus | personal
- `text`: the capture

## Guardrails
- Honors the no-contamination law (root `CLAUDE.md`).
- Honors the `mad` compliance wall (`mad/CLAUDE.md`).
