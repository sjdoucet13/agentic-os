# zus — Domain Router

The **zus-legacy** project (the `nextjs-boilerplate` repo).

## Scope
Work, knowledge, and skills for zus-legacy. Knowledge accrues under `knowledge/` using
the Architecture / Decisions / Lessons / Domain / Gotchas structure.

> Substance is **not yet populated**. Real knowledge is added in the deferred
> ground-truth recon pass (local: `~/projects/zus-legacy`). Until then, `knowledge/`
> holds empty templates only — do not invent facts.

## Isolation declaration
- This domain references **only** zus. It does **not** link to `fugro`, `mad`, or
  `personal`. If something seems shared, **stop and ask** — never bridge.

## Graph
Served by the read-only graph at `_graphs/zus-legacy/` (generated by Graphify; never
hand-edited).

## Layout
- `knowledge/` — walled, accumulating knowledge (5-part structure).
- `skills/` — domain-specific tasks (reusable cross-domain tasks live in `_ops/`).
- `now.md` — the single current focus + next actions.
